# Get one block (columns as expected by kwb.rabimo) for testing

Get one block (columns as expected by kwb.rabimo) for testing

## Usage

``` r
get_test_blocks(codes = c("1100541241000000", "1400761421000000"))
```

## Arguments

- codes:

  codes of the blocks to be selected from the Berlin dataset
