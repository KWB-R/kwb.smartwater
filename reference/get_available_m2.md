# Get available area for each measure, based on current "state"

Get available area for each measure, based on current "state"

## Usage

``` r
get_available_m2(areas)
```

## Arguments

- areas:

  areas in m2, as returned by rabimo_block_to_partial_areas_m2
